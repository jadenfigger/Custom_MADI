- So the simulation simulates the graident directions in three orthogonal directions, then computes the average of these to find the normalized signal. The actual images I have typically use many graident directions, so i compute the spherical mean to determine the normalized signal. Three orthongonal directions is sufficient in the simulation as macroscopically the simulated envirement is isotropic (really i wonder if only one direction could be used and taken as the average). 